Before running, be sure to change the relativeFileRoot variable at the top of the Main.kt main function. 
You can use an absolute or relative path, but if relative is used, ensure it's relative to the directory the program is executing in.
